# Deployment scripts

The final handover package will include the tested Linux service installation, build, start, stop,
health-check and backup/restore scripts used during internal-server verification.

